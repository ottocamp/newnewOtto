#question-query.properties
freqQesCount=SELECT COUNT(*) FROM QES_INDEX WHERE FREQ='Y'
freqTagQesCount=SELECT COUNT(*) FROM QES_INDEX WHERE FREQ='Y' AND Q_TAG = ?
getFreqQesList=SELECT Q_TITLE, Q_CONTENT, Q_TAG, Q_NO FROM (SELECT ROWNUM RNUM, Q_TITLE, Q_CONTENT, Q_TAG, Q_NO FROM (SELECT Q_TITLE, Q_CONTENT, Q_TAG, Q_NO FROM QES_INDEX WHERE FREQ = 'Y' ORDER BY Q_NO DESC) ORDER BY RNUM) WHERE RNUM BETWEEN ? AND ?
getFreqTagQesList=SELECT Q_TITLE, Q_CONTENT, Q_TAG, Q_NO FROM (SELECT ROWNUM RNUM, Q_TITLE, Q_CONTENT, Q_TAG, Q_NO FROM (SELECT Q_TITLE, Q_CONTENT, Q_TAG, Q_NO FROM QES_INDEX WHERE FREQ = 'Y' AND Q_TAG = ? ORDER BY Q_NO DESC) ORDER BY RNUM) WHERE RNUM BETWEEN ? AND ?   

searchFreqCount=SELECT COUNT(*) FROM QES_INDEX WHERE FREQ='Y' AND (Q_TITLE LIKE '%' || ? || '%' OR Q_CONTENT LIKE '%' || ? || '%')
searchTagFreqCount=SELECT COUNT(*) FROM QES_INDEX WHERE FREQ='Y' AND (Q_TITLE LIKE '%' || ? || '%' OR Q_CONTENT LIKE '%' || ? || '%') AND Q_TAG = ?
searchFreqQuestion=SELECT Q_TITLE, Q_CONTENT, Q_TAG, Q_NO FROM (SELECT ROWNUM RNUM, Q_TITLE, Q_CONTENT, Q_TAG, Q_NO FROM (SELECT Q_TITLE, Q_CONTENT, Q_TAG, Q_NO FROM QES_INDEX WHERE FREQ = 'Y' ORDER BY Q_NO DESC) WHERE (Q_TITLE LIKE '%' || ? || '%' OR Q_CONTENT LIKE '%' || ? || '%') ORDER BY RNUM) WHERE RNUM BETWEEN ? AND ?
searchFreqTagQuestion=SELECT Q_TITLE, Q_CONTENT, Q_TAG, Q_NO FROM (SELECT ROWNUM RNUM, Q_TITLE, Q_CONTENT, Q_TAG, Q_NO FROM (SELECT Q_TITLE, Q_CONTENT, Q_TAG, Q_NO FROM QES_INDEX WHERE FREQ = 'Y' AND Q_TAG = ? ORDER BY Q_NO DESC) WHERE (Q_TITLE LIKE '%' || ? || '%' OR Q_CONTENT LIKE '%' || ? || '%') ORDER BY RNUM) WHERE RNUM BETWEEN ? AND ?

getNoMemQuestion=SELECT Q_TITLE, Q_CONTENT, UPDATE_DATE, ANS, ANS_YN, USER_NAME FROM QES_INDEX JOIN MEMBER ON(ANS_WRITER = USER_NO) WHERE FREQ = 'N' AND DISPO_ID = ? AND DISPO_PWD = ?

myQesCount=SELECT COUNT(*) FROM QES_INDEX WHERE QES_WRITER = ?
getMyQuestion=SELECT * FROM (SELECT ROWNUM RNUM, Q.* FROM (SELECT * FROM MY_QES_VIEW WHERE QES_WRITER = ?)Q) WHERE RNUM BETWEEN ? AND ?
serachMyQesCount=SELECT COUNT(*) FROM QES_INDEX WHERE QES_WRITER = ? AND (Q_TITLE LIKE '%' || ? || '%' OR Q_CONTENT LIKE '%' || ? || '%')
searchMyQuestion=SELECT * FROM (SELECT ROWNUM RNUM, Q.* FROM MY_QES_VIEW Q WHERE QES_WRITER = ? AND (Q_TITLE LIKE '%' || ? || '%' OR Q_CONTENT LIKE '%' || ? || '%') ORDER BY RNUM) WHERE RNUM BETWEEN ? AND ?
adminAnswerListCount=SELECT COUNT(*) FROM QES_INDEX JOIN MEMBER ON(USER_NO = ANS_WRITER) WHERE USER_TYPE = 'A' AND FREQ = 'N'
adminAnswerList=SELECT * FROM (SELECT ROWNUM RNUM, Q.* FROM (SELECT Q_TITLE, Q_CONTENT, UPDATE_DATE, QES_WRITER, DISPO_ID, ANS_YN, ANS, Q_NO FROM QES_INDEX JOIN MEMBER ON(USER_NO = ANS_WRITER) WHERE USER_TYPE = 'A' AND FREQ = 'N' ORDER BY ANS_YN DESC, UPDATE_DATE DESC, Q_NO DESC) Q ORDER BY RNUM) WHERE RNUM BETWEEN ? AND ?
getQesWriter=SELECT Q_NO, USER_NO, USER_NAME, DISPO_ID FROM QES_INDEX JOIN MEMBER ON(QES_WRITER = USER_NO) WHERE Q_NO = ?
answerListCount=SELECT COUNT(*) FROM QES_INDEX JOIN MEMBER ON(USER_NO = ANS_WRITER) WHERE ANS_WRITER = ? AND FREQ = 'N'
answerList=SELECT * FROM (SELECT ROWNUM RNUM, Q.* FROM (SELECT Q_TITLE, Q_CONTENT, UPDATE_DATE, QES_WRITER, DISPO_ID, ANS_YN, ANS, Q_NO FROM QES_INDEX JOIN MEMBER ON(USER_NO = ANS_WRITER) WHERE  ANS_WRITER = ? AND FREQ = 'N' ORDER BY ANS_YN DESC, UPDATE_DATE DESC, Q_NO DESC) Q ORDER BY RNUM) WHERE RNUM BETWEEN ? AND ?
searchAdminCount=SELECT COUNT(*) FROM QES_INDEX JOIN MEMBER ON(USER_NO = ANS_WRITER) WHERE USER_TYPE = 'A' AND FREQ = 'N' AND (Q_CONTENT LIKE '%' || ? || '%' OR Q_TITLE LIKE '%' || ? || '%')
searchListCount=SELECT COUNT(*) FROM QES_INDEX JOIN MEMBER ON(USER_NO = ANS_WRITER) WHERE ANS_WRITER = ? AND FREQ = 'N' AND (Q_CONTENT LIKE '%' || ? || '%' OR Q_TITLE LIKE '%' || ? || '%')
searchAdminList=SELECT * FROM (SELECT ROWNUM RNUM, Q.* FROM (SELECT Q_TITLE, Q_CONTENT, UPDATE_DATE, QES_WRITER, DISPO_ID, ANS_YN, ANS, Q_NO, USER_NAME FROM QES_INDEX JOIN MEMBER ON(USER_NO = ANS_WRITER) WHERE USER_TYPE = 'A' AND FREQ = 'N' ORDER BY ANS_YN DESC, UPDATE_DATE DESC, Q_NO DESC) Q WHERE (Q_CONTENT LIKE '%' || ? || '%' OR Q_TITLE LIKE '%' || ? || '%') ORDER BY RNUM) WHERE RNUM BETWEEN ? AND ?
searchAnswerList=SELECT * FROM (SELECT ROWNUM RNUM, Q.* FROM (SELECT Q_TITLE, Q_CONTENT, UPDATE_DATE, QES_WRITER, DISPO_ID, ANS_YN, ANS, Q_NO, USER_NAME FROM QES_INDEX JOIN MEMBER ON(USER_NO = ANS_WRITER) WHERE ANS_WRITER = ? AND FREQ = 'N' ORDER BY ANS_YN DESC, UPDATE_DATE DESC, Q_NO DESC) Q WHERE (Q_CONTENT LIKE '%' || ? || '%' OR Q_TITLE LIKE '%' || ? || '%') ORDER BY RNUM) WHERE RNUM BETWEEN ? AND ?

insertAnswer=UPDATE QES_INDEX SET ANS = ?, ANS_YN = 'O' WHERE Q_NO = ?
insertQuestion=INSERT INTO QES_INDEX VALUES(SEQ_Q_NO.NEXTVAL, ?, ?, DEFAULT, 9, DEFAULT, DEFAULT, ?, ?, ?, ?, 'N')
checkIdPwd=SELECT COUNT(*) FROM QES_INDEX WHERE DISPO_ID = ?
